name: Deploy to EC2 and run E2E

on:
  workflow_dispatch:

jobs:
  deploy-and-test-ec2:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build jar
        run: mvn -DskipTests package

      - name: Create AMI or Run Instance
        run: |
          echo "EC2 deployment steps should be added here."
          echo "This can use user-data or an AMI to run the container/Java app."

      - name: Run Playwright tests
        run: |
          echo "Run Playwright tests against the EC2 public URL (set TEST_BASE_URL secret)"
